{
	"version": 2,
	"buildCommand": "npm run build:ssr",
	"outputDirectory": "dist/zyra-ui",
	"framework": null,
	"routes": [
		{
			"handle": "filesystem"
		},
		{
			"src": "/(.*)",
			"dest": "/server/server.mjs"
		}
	]
}