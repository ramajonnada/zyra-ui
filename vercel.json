{
    "version": 2,
    "buildCommand": "npm run build:ssr",
    "outputDirectory": "dist/zyra-ui/browser",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}
